# Backend Testing

To run backend FastAPI tests:

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run tests from the project root:
   ```bash
   pytest
   ```

Tests are located in the `tests/` directory and follow the Arrange-Act-Assert (AAA) pattern for clarity and maintainability.
